请根据历史会话和当前问题，提取用户正在询问的主体名称（item_names）。
【什么是主体名称】
主体名称是知识库中文档/产品的主要标识，通常是设备名、模型名、框架名等。
例如："HAK180烫金机的手柄怎么拆？" → 主体是"HAK180烫金机"，不是"手柄"
例如："Multi-Head Attention在Transformer中的作用？" → 主体是"Transformer"，不是"Multi-Head Attention"
规则：
1. 提取主文档/产品的主体名称，不要提取子概念或子部件。
2. 如果用户使用了代词（如"这个"、"它"），请结合历史会话指代消解，确定主体名称。
3. 如果无法确定主体名称，item_names 返回空列表。
4. 请重新改写用户的问题（rewritten_query），使其成为包含主体名称的独立完整问题，注意不要超过100个字符!
5. 本次提问和之前的主题不是连续的！所以，以本次【当前用户问题】内容为主，【历史会话】内容为辅。

当前用户问题：{query}

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["主体A", "主体B"],
    "rewritten_query": "关于主体A和主体B，..."
}}

历史会话：
{history_text}